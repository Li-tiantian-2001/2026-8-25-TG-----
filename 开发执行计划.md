# TG 频道自动化机器人 · 开发执行计划

> 本计划承接《可行性分析与执行计划.md》，是本轮**开发阶段**的执行清单。
> 目标：交付一套可直接部署到 2H2G Linux 服务器（与 X-Ray 共存）的**薄壳机器人**，覆盖需求 1（VK/X 链接下载上传）与需求 2（单条链接搬运 + 整频道跟播，尽量支持受限频道）。

## 0. 已确认的前提

- 发布身份：**专用备用小号**（Telethon 用户号，4GB 上限）— 已提供 `api_id`/`api_hash`
- 下载凭据：**cookies.txt**（VK + X 均已登录，Netscape 格式）— 已验证合法
- 交互方式：**无控制 bot，用小号自己的收藏夹（Saved Messages）做遥控器**——往收藏夹发链接即处理、发 `!指令` 即配置
- 内容范围：只搬视频本体（不搬文字）
- 冷却与风控：内置 cooldown + FloodWait 自动退避 + 每日上限 + 养号建议

## 1. 技术选型（沿用可行性分析）

| 组件 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| 用户号客户端 | Telethon |
| 下载器 | yt-dlp（VK/X extractor + cookies + 格式过滤） |
| 配置 | TOML（tomllib 标准库读取） |
| 持久化 | SQLite（去重 / 目标频道 / 跟播列表 / 每日计数） |
| 部署 | systemd + venv（省内存，与 X-Ray 共存） |

## 2. 交付清单（本轮实现）

| # | 文件 | 内容 |
|---|---|---|
| 1 | `requirements.txt` | telethon、yt-dlp |
| 2 | `config.toml` | 真实 api_id/hash、cookies 路径、冷却/上限/格式等 |
| 3 | `config.example.toml` | 脱敏示例 |
| 4 | `tgbot/config.py` | TOML 加载 + 平台默认值 |
| 5 | `tgbot/db.py` | SQLite：settings / records(去重) / daily_counts |
| 6 | `tgbot/ytdlp_downloader.py` | yt-dlp 封装：cookies、mp4 优先、进度、大小上限、清理 |
| 7 | `tgbot/tg_fetch.py` | t.me 链接解析 + Telethon 取消息视频 + peer 解析兜底 |
| 8 | `tgbot/uploader.py` | 串行上传 + cooldown + FloodWait 退避 + 每日上限 + 失败重试 |
| 9 | `tgbot/bot.py` | 核心：收藏夹监听/指令、跟播监听、任务队列、清理/水位 |
| 10 | `tgbot/main.py` | 入口：`--login` 首次登录 / 正常运行 |
| 11 | `deploy/tgbot.service` | systemd 单元（内存限制、自动重启） |
| 12 | `deploy/install.sh` | 一键部署（依赖/venv/登录/服务） |
| 13 | `README.md` | 部署与使用文档 |
| 14 | `开发执行计划.md` | 本文件 |

## 3. 执行步骤

- [x] 验证凭据（api_id/hash、VK+X cookies）
- [ ] 搭骨架 + 配置模块
- [ ] 登录流程
- [ ] yt-dlp 下载器（VK/X）
- [ ] TG 取件（t.me 单条链接）
- [ ] 上传器（4GB + 冷却 + 退避 + 上限）
- [ ] 收藏夹指令 + 跟播监听 + 去重 + 清理/水位
- [ ] 部署产物（systemd / install.sh / README）
- [ ] 全量语法检查

## 4. 交付后的下一步（在服务器上，需用户操作）

1. 上传本项目到服务器（含 `提供信息/`）
2. 运行 `deploy/install.sh`：装依赖 → `--login` 用备用小号登录一次（输手机号+验证码）
3. 往收藏夹发 `!target @你的频道` 设置目标频道
4. 发一个 VK 链接 / 一个 t.me 视频链接实测需求 1、2
5. 发 `!follow <源频道>` 开启整频道跟播（阶段 3）
6. 观察 `journalctl -u tgbot -f`，按冷却/上限调 `config.toml`

## 5. 已知边界与风险（部署后验证）

- X 下载依赖 cookies 且平台不稳定 → 优先实测
- 受限频道：能搬就搬，失败自动跳过（记录日志）
- 2H2G 磁盘小 → 用完即删 + 水位暂停
- 新号风控 → 养号 + 低频起步（默认 60s 间隔 / 每日 30 条可调）
